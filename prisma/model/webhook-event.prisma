model WebhookEvent {
    id        String    @id @default(uuid()) @map("_id")
    eventId   String   @unique
    provider  String // paystack
    eventType String // refund.processed, refund.failed
    data      Json
    createdAt DateTime @default(now())

    @@map("webhook_events")
}
