model Dispute {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  errandId  String
  clientId  String
  workerId  String
  status    DisputeStatus
  reason    String
  createdAt DateTime @default(now())

  @@map("disputes")
}

enum DisputeStatus {
  PENDING
  ACCEPTED
  REJECTED
}
